API_URL=http://localhost:4100
WEB_REVALIDATE_SECRET=change-me-revalidate

# Only needed if CMS-uploaded images (member logos, team photos, etc.) are
# served from a non-default host/port — see next.config.ts.
# ASSET_HOST_HOSTNAME=localhost
# ASSET_HOST_PORT=4100
